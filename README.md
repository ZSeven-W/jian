# Jian (简)

A Rust-native cross-platform UI framework. An `.op` file is an app.

See `docs/` for the design spec (pending publication).

## Crates

- `jian-ops-schema` — canonical schema + types for `.op` files. Generates JSON Schema and TypeScript bindings.

## License

MIT
